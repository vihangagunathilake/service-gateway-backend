<configuration>

    <!-- Default fallback (used if LOG_PATH is not set) -->
    <property name="LOG_PATH" value="${LOG_PATH:-logs}"/>

    <!-- Console -->
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>SERVICE GATEWAY %d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File logging (ONLY for production) -->
    <springProfile name="production">
        <appender name="file" class="ch.qos.logback.core.rolling.RollingFileAppender">

            <file>${LOG_PATH}/service-gateway.log</file>

            <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
                <fileNamePattern>${LOG_PATH}/service-gateway.%d{yyyy-MM-dd}.log</fileNamePattern>
                <maxHistory>7</maxHistory>
            </rollingPolicy>

            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
            </encoder>

        </appender>
    </springProfile>

    <!-- Root logger for DEVELOPMENT -->
    <springProfile name="development">
        <root level="INFO">
            <appender-ref ref="console"/>
        </root>
    </springProfile>

    <!-- Root logger for PRODUCTION -->
    <springProfile name="production">
        <root level="INFO">
            <appender-ref ref="console"/>
            <appender-ref ref="file"/>
        </root>
    </springProfile>

</configuration>